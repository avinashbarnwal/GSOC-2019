OS := $(shell uname)
ifeq ($(OS), Darwin)
	OUTPUT_LIB = distlib.dylib
else
	OUTPUT_LIB = distlib.so
endif

all: $(OUTPUT_LIB)

$(OUTPUT_LIB): dlogis.cpp dnorm.cpp plogis.cpp pnorm.cpp grad_norm.cpp
	$(CXX) -O3 -shared -o $@ $^
